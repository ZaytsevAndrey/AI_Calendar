import React from 'react';
import EmailVerificationForm from 'modules/auth/components/EmailVerificationForm';

const EmailVerificationPage = () => {
    return <EmailVerificationForm />;
};

export default EmailVerificationPage;
