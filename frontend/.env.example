# Backend base URL (see src/api/axios.ts).
# Local: http://localhost:3001
# Production (Render): https://<ai-calendar-api>.onrender.com  — set at build time
REACT_APP_API_BASE_URL=http://localhost:3001
